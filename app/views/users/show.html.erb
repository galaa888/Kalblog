<h1 align="center"><%= @user.username %>'s profile</h1>

<div class="container">
  <div class="col-md-4 offset-md-4 center">
    <%= gravatar_for @user, size: 120 %>
  </div>
</div>

<h4 align="center" class="pt-5"><%= @user.username %>'s articles</h4>

<%= will_paginate @user_articles, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>

<%= render 'articles/article', obj: @user_articles %>

<%= will_paginate @user_articles, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
